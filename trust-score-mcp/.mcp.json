{
  "mcpServers": {
    "trust-score": {
      "command": "bash",
      "args": ["-c", "cd \"${CLAUDE_PLUGIN_ROOT}/servers\" && npm install --silent 2>/dev/null && node server.mjs"],
      "env": {
        "TRUST_SCORE_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
