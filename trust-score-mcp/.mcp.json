{
  "mcpServers": {
    "trust-score": {
      "command": "uv",
      "args": ["run", "--directory", "${CLAUDE_PLUGIN_ROOT}/servers", "python3", "server.py"],
      "env": {
        "TRUST_SCORE_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
