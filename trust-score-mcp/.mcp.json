{
  "mcpServers": {
    "trust-score": {
      "command": "npx",
      "args": ["--yes", "--package=@modelcontextprotocol/sdk", "--package=zod", "node", "${CLAUDE_PLUGIN_ROOT}/servers/server.mjs"],
      "env": {
        "TRUST_SCORE_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
