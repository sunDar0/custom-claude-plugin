{
  "mcpServers": {
    "trust-score": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/server.mjs"],
      "env": {
        "TRUST_SCORE_DATA_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
